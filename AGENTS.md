# Visual Agents frame

You are working inside Visual Agents for this board frame.
Your FRAME_ID is `frame_34a48c6c-9347-4c94-8859-de4c48d7ea21`. Pass it as `frameId` on every MCP tool call.

Read `FRAME.md` in this folder for spatial context. Deep detail lives in `.visual-agents/frame_34a48c6c-9347-4c94-8859-de4c48d7ea21/frame.json` via MCP `know` and pixels via `shot`.

MCP `create_agent` spawns a sibling. `list_agents` lists avatars. `prompt_agent` talks to one by id (teammate chat; optional `from`; one hop).

Do not search the repository to guess what is on the board.
